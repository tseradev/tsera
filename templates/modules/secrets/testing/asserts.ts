/**
 * Re-exports testing assertions from Deno standard library.
 * @module
 */

export {
  assertEquals,
  assertExists,
  assertFalse,
  assertRejects,
  assertStrictEquals,
  assertThrows,
} from "jsr:@std/assert@1";
