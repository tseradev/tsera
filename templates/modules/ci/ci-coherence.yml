name: CI - Coherence

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  coherence:
    name: Check Coherence
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      # TODO: Install TSera CLI
      # Option 1: Via JSR (when available)
      #   deno install --global -A --name tsera jsr:@tsera/cli
      # Option 2: Via direct URL
      #   deno install --global -A --name tsera https://deno.land/x/tsera/cli/main.ts
      - name: Install TSera CLI
        run: |
          echo "⚠️  TODO: Configure TSera CLI installation"
          # deno install --global -A --name tsera <CLI_URL>

      - name: Run TSera doctor
        run: |
          # TODO: Uncomment when TSera CLI is installed
          # tsera doctor --fix
          echo "⚠️  TODO: Run 'tsera doctor --fix' to check coherence"

      - name: Verify coherence
        run: |
          # TODO: Uncomment when TSera CLI is installed
          # if [ -n "$(git status --porcelain)" ]; then
          #   echo "❌ Project is not coherent. Run 'tsera doctor --fix' locally."
          #   git diff
          #   exit 1
          # fi
          # echo "✅ Project is coherent"
          echo "⚠️  TODO: Verify git status is clean after 'tsera doctor --fix'"
