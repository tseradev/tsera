/**
 * Placeholder artifact generator for smoke tests.
 */

export async function emitSmokeTest(name: string): Promise<void> {
  await Promise.resolve();
  console.log(`Génération test (placeholder): ${name}`);
}
